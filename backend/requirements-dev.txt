# Test and tooling dependencies. Kept out of requirements.txt so the production
# image built by backend/Dockerfile does not ship a test runner.
-r requirements.txt
pytest==8.2.2
pytest-asyncio==0.23.7
